rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // MyActivity menggunakan Firebase Admin SDK dari Cloud Functions.
    // Custom session JWT tidak pernah memberi akses langsung ke browser.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

